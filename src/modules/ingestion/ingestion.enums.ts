export enum FuelUnits {
    GALLONS = "gallons",
    LITERS = "liters",
}

export enum SpeedUnits {
    MILES = "miles",
    KILOMETERS = "kilometers",
}

export enum EngineTemperatureUnits {
    CELSIUS = "C",
    FAHRENHEIT = "F",
}
